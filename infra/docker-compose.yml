services:
  postgres:
    image: postgres:16
    container_name: babylog-postgres
    environment:
      POSTGRES_DB: babylog
      POSTGRES_USER: household
      POSTGRES_PASSWORD: household
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
